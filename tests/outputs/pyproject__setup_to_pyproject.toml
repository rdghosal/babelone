[project]
name = "babelone-test-app"
version = "2.0"
dependencies = [
    "pydantic==2.6.2",
    "fastapi",
]

[project.optional-dependencies]
PDF = [
    "ReportLab>=1.2",
    "RXP",
]
dev = [
    "pytest",
    "hypothesis>=6.95.x",
]

[project.scripts]
hello-world = "timmins:hello_world"

[project.gui-scripts]
hello-world = "timmins:hello_world"
