[project]
name = "hello-world-app"
version = "2.0"
dependencies = [
    "pydantic==2.6.2",
    "fastapi",
]

[project.optional-dependencies]
dev = [
    "pytest",
    "hypothesis>=6.95.x",
]
PDF = [
    "ReportLab>=1.2",
    "RXP",
]

[project.scripts]
hello-world = "timmins:hello_world"

[project.gui-scripts]
hello-world = "timmins:hello_world"
